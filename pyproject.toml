[project]
name = "pico-backend"
version = "0.1.0"
description = ""
authors = [
    {name = "Luiz Felipe Neves",email = "luizfneves@proton.me"}
]
requires-python = ">=3.12,<4.0"
dependencies = [
    "sqlalchemy (>=2.0.38,<3.0.0)",
    "asyncpg (>=0.30.0,<0.31.0)",
    "bcrypt (>=4.3.0,<5.0.0)",
    "pydantic-settings (>=2.8.1,<3.0.0)",
    "pyjwt (>=2.10.1,<3.0.0)",
    "sqlalchemy-utils (>=0.41.2,<0.42.0)",
    "pydantic-extra-types (>=2.10.2,<3.0.0)",
    "boto3 (>=1.37.3,<2.0.0)",
    "pydantic[email] (>=2.10.6,<3.0.0)",
    "phonenumbers (>=8.13.55,<9.0.0)", # Needed for pydantic phone numbers
    "redis (>=5.2.1,<6.0.0)",
    "arq (>=0.26.3,<0.27.0)",
    "python-multipart (>=0.0.20,<0.0.21)", # Needed for FastAPI request files
    "sqladmin[full] (>=0.20.1,<0.21.0)",
    "openai (>=1.68.2,<2.0.0)",
    "tiktoken (>=0.9.0,<0.10.0)",
    "pylatexenc (>=2.10,<3.0)",
    "amplitude-analytics (>=1.1.5,<2.0.0)",
    "factory-boy (>=3.3.3,<4.0.0)",
    "pytest (>=8.3.5,<9.0.0)",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.app.dependencies]
fastapi = {extras = ["standard"], version = ">=0.115.10,<0.116.0"}
uvicorn = {extras = ["standard"], version = ">=0.34.0,<0.35.0"}
alembic = ">=1.14.1,<2.0.0"

[tool.poetry.group.test.dependencies]
fastapi = {extras = ["standard"], version = ">=0.115.10,<0.116.0"}
uvicorn = {extras = ["standard"], version = ">=0.34.0,<0.35.0"}
alembic = ">=1.14.1,<2.0.0"
pytest = "^8.3.4"
pytest-xdist = "^3.6.1"
httpx = ">=0.28.1,<0.29.0"

[tool.poetry.group.dev.dependencies]
sqlacodegen = {extras = ["pgvector"], version = "^3.0.0"}
psycopg = "^3.2.6"

